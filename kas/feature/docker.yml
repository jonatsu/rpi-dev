# yaml-language-server: $schema=https://raw.githubusercontent.com/siemens/kas/master/kas/schema-kas.json
header:
  version: 20
  includes:
    - kas/extra/virtualization.yml

local_conf_header:
  00_enable-docker: |
    IMAGE_INSTALL:append = " docker-ce"
    DISTRO_FEATURES:append = " docker"
